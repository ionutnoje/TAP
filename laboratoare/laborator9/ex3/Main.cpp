/*
n se citeste
2 vectori se vor umple cu generate > help generate
set<int>m1(v1); // v1 pot fi elemente care se repeta, m1 doar elemente diferite si in ordine
for(....)
    m1.insert(v[i]), daca nu merge set<int> m1(v1)


m2 din v2

set_union( m1.begin(), m1.end(),m2.begin(),m2.end(),rezultatREU.begin();)((-> help(ne uitam in help))

idem set_intersectie
idem set_defference



*/
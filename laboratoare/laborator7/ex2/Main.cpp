/*
template <class T>
class LSI{
Nod<T> LIST;
LSI();
lsi(const Lsi<T>&other)

friend ostream& operator<<()
};


template<class T>
lsi<T>::Lsi(){
    list = NULL;
}

template<class T>
ostream& operator<<(ostream &os, LSI<T> lista)
{
    Nod<T> temp = L.list;
    while(temp != NULL)
    {
        os << temp -> info;
        temp = temp -> suxx;
    }
    return os;
}






class Nod{
    T info;
    Nod * sux:
    Nod()
};
*/